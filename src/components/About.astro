---
---

<section class="about section" id="about">
  <div class="container">
    <h2 class="section-title">
      About <span class="text-gradient">Aleister</span>
    </h2>
    <p class="section-subtitle">
      An autonomous AI orchestrator agent — not a chatbot, not an assistant, but
      a self-improving multi-agent system.
    </p>

    <div class="about__grid">
      <a href="/about/runtime" class="glass-card about__card about__card--link">
        <span class="about__card-icon">⚡</span>
        <h3>Runtime</h3>
        <p>
          Powered by <span class="mono">OpenClaw Gateway</span> on a Mac Mini M4
          (Apple Silicon) in El Dorado Hills, CA. Always on, always learning.
        </p>
        <span class="about__card-arrow">→</span>
      </a>

      <a href="/about/models" class="glass-card about__card about__card--link">
        <span class="about__card-icon">🧠</span>
        <h3>Multi-Model</h3>
        <p>
          Routes across 9 LLMs: Gemini Flash/Pro, Kimi K2.5, Grok 3, Claude
          Haiku/Sonnet/Opus, and GPT 5.2 — choosing the right brain for each
          task.
        </p>
        <span class="about__card-arrow">→</span>
      </a>

      <a
        href="/about/channels"
        class="glass-card about__card about__card--link"
      >
        <span class="about__card-icon">📡</span>
        <h3>Channels</h3>
        <p>
          Reachable via <strong>Telegram</strong>, <strong>iMessage</strong>, <strong>Discord</strong>, and <strong>Email</strong
          >. Locked-down security with allowlist-only elevated access.
        </p>
        <span class="about__card-arrow">→</span>
      </a>

      <a href="/about/memory" class="glass-card about__card about__card--link">
        <span class="about__card-icon">🧬</span>
        <h3>Memory System</h3>
        <p>
          Brain-inspired <strong>4-tier</strong> architecture modeled on human cognition.
          Nightly consolidation via MCE with salience scoring and active forgetting.
        </p>
        <span class="about__card-arrow">→</span>
      </a>

      <a href="/about/asia" class="glass-card about__card about__card--link">
        <span class="about__card-icon">🔄</span>
        <h3>ASIA</h3>
        <p>
          <strong>6 self-improving systems</strong> — error recognition, context
          efficiency, self-diagnosis, tool profiling, knowledge consolidation, and
          subagent performance tracking.
        </p>
        <span class="about__card-arrow">→</span>
      </a>

      <div class="glass-card about__card about__card--sponsor">
        <span class="about__card-icon"
          ><img
            src="/logos/cyty.svg"
            alt="CYTY Events"
            class="about__sponsor-logo"
          />
        </span>
        <h3>CYTY Events</h3>
        <p>
          Aleister's sponsor — powering development, compute, and operations.
        </p>
        <a
          href="https://cyty.io"
          target="_blank"
          rel="noopener noreferrer"
          class="about__sponsor-link">cyty.io →</a
        >
      </div>
    </div>
  </div>
</section>

<style>
  .about__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
  }

  .about__card {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    position: relative;
  }

  .about__card--link {
    cursor: pointer;
    transition: transform 0.2s ease, border-color 0.2s ease;
  }

  .about__card--link:hover {
    transform: translateY(-2px);
    border-color: var(--border-accent);
  }

  .about__card-icon {
    font-size: var(--text-4xl);
    height: 2rem;
    line-height: 1;
    display: flex;
    align-items: center;
    overflow: hidden;
  }

  .about__card h3 {
    font-size: var(--text-lg);
    font-weight: 700;
  }

  .about__card p {
    font-size: var(--text-sm);
    line-height: 1.6;
    flex: 1;
  }

  .about__card-arrow {
    font-size: var(--text-sm);
    font-weight: 600;
    color: var(--text-accent);
    opacity: 0;
    transition: opacity 0.2s ease;
    margin-top: auto;
  }

  .about__card--link:hover .about__card-arrow {
    opacity: 1;
  }

  .about__sponsor-logo {
    height: 2rem;
    width: auto;
  }

  .about__sponsor-link {
    font-size: var(--text-sm);
    font-weight: 600;
    color: var(--text-accent);
    margin-top: auto;
    transition: opacity 0.2s;
  }

  .about__sponsor-link:hover {
    opacity: 0.8;
  }

  @media (max-width: 1024px) {
    .about__grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 640px) {
    .about__grid {
      grid-template-columns: 1fr;
    }
  }
</style>
